{
  "type": "object",
  "title": "Issue 79",
  "description": "x-buttons on an array editor. Clicking Fill replaces the rows with a single filled item - it must keep working even after the array's refreshUI() has run (e.g. after adding/removing an item).",
  "properties": {
    "rows": {
      "type": "array",
      "title": "Rows",
      "x-format": "nav-horizontal",
      "x-buttons": [
        {
          "label": "Fill",
          "event": {
            "name": "fill"
          }
        }
      ],
      "items": {
        "type": "object",
        "properties": {
          "a": {
            "type": "string",
            "title": "A"
          }
        }
      }
    }
  }
}
