{
  "title": "[Issue #11](/index.html?example=issue/issue-11)",
  "type": "object",
  "properties": {
    "input": {
      "type": "string",
      "maxLength": 10,
      "minLength": 5,
      "pattern": "^[a-z]*$",
      "description": "{{ length }}/10 characters ({{ remaining }} left)",
      "x-showErrors": "input",
      "x-messages": {
        "pattern": "Must contain only lowercase letters"
      }
    },
    "textarea": {
      "type": "string",
      "x-format": "textarea",
      "maxLength": 10,
      "minLength": 5,
      "description": "{{ length }}/10 characters ({{ remaining }} left)",
      "x-showErrors": "input"
    }
  }
}
