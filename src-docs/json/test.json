{
  "title": "Form Example",
  "type": "object",
  "properties": {
    "A": {
      "title": "Element A",
      "type": "string",
      "enum": [
        "Large",
        "Medium",
        "Small"
      ],
      "default": "Medium",
      "x-format": "select"
    },
    "B": {
      "title": "Element B",
      "type": "string",
      "x-format": "input"
    },
    "C": {
      "title": "Element C",
      "type": "string",
      "enum": [
        "Large",
        "Medium",
        "Small"
      ],
      "default": "Medium",
      "x-format": "select"
    },
    "D": {
      "title": "Element D",
      "type": "string",
      "x-format": "input"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "A": {
            "const": "Large"
          }
        },
        "required": [
          "A"
        ]
      },
      "then": {
        "properties": {
          "B": {
            "type": "string",
            "x-format": "input"
          }
        },
        "required": [
          "B"
        ]
      },
      "else": {
        "properties": {
          "B": {
            "type": "string",
            "x-hidden": true
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "A": {
            "const": "Large"
          }
        },
        "required": [
          "A"
        ]
      },
      "then": {
        "properties": {
          "D": {
            "type": "string",
            "x-format": "input"
          }
        },
        "required": [
          "D"
        ]
      },
      "else": {
        "properties": {
          "D": {
            "type": "string",
            "x-hidden": true
          }
        }
      }
    }
  ]
}